# Public site URL (used by metadataBase, sitemap, OG). Set to the deployed URL in production.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# ---------------------------------------------------------------------------
# The keys below are NOT required for Phase 1. They are documented here so the
# architecture is clear. They are wired up in the phases noted.
# ---------------------------------------------------------------------------

# AI Playground (Phase 6) — server-side only, never exposed to the client.
# ANTHROPIC_API_KEY=
# OPENAI_API_KEY=
# AI_RATE_LIMIT_PER_HOUR=20

# GitHub section (Phase 7)
# GITHUB_TOKEN=
# GITHUB_USERNAME=

# Contact form (Phase 7)
# CONTACT_WEBHOOK_URL=
